# Production frontend configuration

# If frontend and backend are served from the same domain with /api routed
# to the backend, leave this empty and the app will use /api automatically.
REACT_APP_BACKEND_URL=

# Example for separate backend hosting:
# REACT_APP_BACKEND_URL=https://api.yourdomain.com
